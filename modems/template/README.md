# Template Modem

Use this directory as the starting point for new modem plugins. Copy the folder, rename it, and implement `encode`/`decode` while updating the schema and defaults JSON files to reflect the plugin parameters.
